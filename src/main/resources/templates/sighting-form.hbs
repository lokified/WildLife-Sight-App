{{#partial "content"}}
    {{#if editSight}}
        <h3>Update Sighting</h3>
        <form action="/sightings/{{editSight.id}}/update" method="post">
            <label for="animal" class="in-line">Animal:</label>
            <select id="animal" name="animal" type="text" class="form-control">
                {{#each animals}}
                    <option value="{{id}}">{{name}}<span class="text-secondary">({{type}})</span></option>
                {{/each}}

            </select>

            <label for="location">Location:</label>
            <select id="location" name="location" type="text" class="form-control">
                <option value="Zone A">Zone A</option>
                <option value="Zone B">Zone B</option>
                <option value="Zone C">Zone C</option>
                <option value="Zone D">Zone D</option>
            </select>

            <label for="rangerName">Ranger Name:</label>
            <input type="text" name="rangerName" class="form-control transparentInputFields" value="{{editSight.rangerName}}">

            <button class="btn btn-success" type="submit">Edit</button>

    {{else}}
            <h1>Record your sightings</h1>

        <form action="/sighting-form/new" method="post">
                <label for="animal" class="in-line">Animal:</label>
                <select id="animal" name="animal" type="text" class="form-control">
                    {{#each animals}}
                        <option value="{{id}}">{{name}}<span class="text-secondary">({{type}})</span></option>
                    {{/each}}

                </select>

                <label for="location">Location:</label>
                <select id="location" name="location" type="text" class="form-control">
                    <option value="Zone A">Zone A</option>
                    <option value="Zone B">Zone B</option>
                    <option value="Zone C">Zone C</option>
                    <option value="Zone D">Zone D</option>
                </select>

                <label for="rangerName">Ranger Name:</label>
                <input type="text" name="rangerName" class="form-control transparentInputFields">

                <button class="btn btn-success" type="submit">Add Sighting</button>

    {{/if}}
    </form>



{{/partial}}
{{> layout.hbs}}